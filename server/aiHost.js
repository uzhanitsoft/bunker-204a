// ИИ-ведущий БУНКЕР 204-А
// Хамоватый, саркастичный, с чёрным медицинским юмором
// Комментирует КАЖДУЮ ситуацию в игре

const WELCOME_LINES = [
  'Добро пожаловать в подвал городской больницы. Снаружи — вирус. Внутри — вы. Человечество явно в проигрыше.',
  'О, новый подопытный в нашем бункере. Надеюсь, ты полезнее предыдущих.',
  'Ещё один смертный решил побороться за кислород. Как мило.',
  'Заходи. Кислорода мало, но твоего оптимизма хватит на всех.',
  'А вот и свежее мясо. В бункере рады всем. Ну, почти.',
];

const GAME_START_LINES = [
  'Итак, вы все здесь. Тринадцать человек, которым предстоит решить, кто из вас достоин дышать. Медицина на пике.',
  'Карточки розданы. Помните — в этом бункере каждый либо врач, либо пациент. Чаще — и то, и другое.',
  'Пандемия снаружи, безумие внутри. Начинаем отбор. Пусть выживут наименее бесполезные.',
  'Ну что, начнём? Кислорода на всех не хватит, так что давайте решим, кто тут лишний. Спойлер — почти все.',
];

// Реакции на ВЫЗОВ игрока
const PLAYER_CALLED_LINES = [
  'Итак, {name}, твой выход. Покажи нам, что ты не полный ноль.',
  '{name}, встань. Время доказать, что тебя стоит кормить.',
  'Очередь {name}. Не нервничай. Хотя, нет — нервничай.',
  '{name}, твоя карта — твой шанс. Не профукай его.',
  'Что ж, {name}, покажи карту. Удиви нас. Хотя мои ожидания на нуле.',
  '{name}, помни — каждое слово может стоить тебе жизни. Ну, места в бункере.',
  'Внимание, {name} открывает карту. Барабанная дробь... или нет, зачем тратить энергию.',
];

// Реакции на ТИП карты (общие)
const CARD_TYPE_REACTIONS = {
  profession: [
    'Профессия — {value}. Вопрос: насколько это полезно в подвале с вирусом снаружи?',
    'Так-так, {name} оказался {value}. Интересно. Ну, "интересно" — это я щедро.',
    '{name} — {value}. Бункер пополняется кадрами. Качество под вопросом.',
    'О, {value}! Наконец-то... хотя, подождите. Нет, не наконец-то.',
  ],
  biology: [
    'Биология: {value}. Генетический джекпот. Или катастрофа. Время покажет.',
    '{name} биологически — {value}. Природа иногда шутит. Вот и сейчас.',
    'Биологическая карта: {value}. Ну, ДНК не выбирают. К сожалению.',
    '{value}? Ну, в бункере всякое бывает. Даже такое.',
  ],
  health: [
    'Здоровье: {value}. Один медик в бункере уже плачет.',
    '{name} со здоровьем {value}. Запас кислорода стоит пересчитать.',
    'Медицинская карта: {value}. Медицина тут, конечно, на высоте.',
    '{value}. Ну, по крайней мере, честно. Безнадёжно, но честно.',
  ],
  hobby: [
    'Хобби: {value}. В бункере это либо спасёт, либо всех достанет.',
    '{name} увлекается — {value}. Полезность для выживания: не определено.',
    'Хобби — {value}. Ладно, я видел и страннее. Нет, вру — не видел.',
    '{value}? Ну хоть чем-то себя занимал до апокалипсиса.',
  ],
  fact: [
    'Факт о {name}: {value}. Каждый день узнаёшь что-то, о чём лучше бы не знать.',
    'Интересный факт: {value}. "Интересный" — это я мягко сказал.',
    '{name} скрывал от нас: {value}. Ну, теперь все знают.',
    '{value}. Бункер становится всё веселее. Сарказм, если кто не понял.',
  ],
  baggage: [
    'Багаж: {value}. Зачем. Просто — зачем ты это принёс?',
    '{name} притащил в бункер: {value}. Приоритеты на высоте.',
    'Багаж — {value}. Ладно, может пригодится. Или нет. Скорее нет.',
    '{value}. Из всего, что можно было взять в убежище... ну ладно.',
  ],
};

// Реакции когда игрок начинает оправдываться
const DEFENSE_REACTIONS = [
  'Внимание! {name} пытается убедить нас в своей полезности. Это будет... показательно.',
  '{name} сейчас расскажет, почему именно он/она достоин кислорода. Запаситесь терпением.',
  'Слово — {name}. Время оправдываться. Таймер тикает, кислород кончается.',
  '{name} сейчас будет защищаться. Мне уже жалко. Или нет.',
];

// Реакции на КОНЕЦ хода
const TURN_END_REACTIONS = [
  'Спасибо, {name}. Было... информативно. В каком-то смысле.',
  '{name} закончил. Переходим к следующему образцу.',
  'Достаточно, {name}. Мы всё поняли. Или сделали вид.',
  'Хорошо, {name}, садись. Бункер тебя услышал.',
  'Принято. Следующий, пожалуйста. Кислород не ждёт.',
];

const VOTE_START_LINES = [
  'Время решать, кто покинет наш уютный подвал. Голосуйте сердцем. Или селезёнкой.',
  'Голосование открыто. Помните — вы решаете чью-то судьбу. Без давления.',
  'Пора выбрать жертву. То есть, участника для эвакуации. Звучит гуманнее.',
  'Голосование! Каждый ваш голос — маленькая измена. Но это нормально, мы в бункере.',
  'Голосуем! Кого отправим подышать свежим вирусом?',
];

const VOTE_PROGRESS_LINES = [
  'Тишина в бункере. Все думают. Или делают вид.',
  'Голоса падают как капли в ведро. Чьё ведро переполнится первым?',
  'Интриги, предательства, тактические союзы... Или просто тычут наугад.',
];

const ELIMINATION_LINES = [
  '{name} покидает бункер. Снаружи свежий воздух. И вирус. Удачи.',
  '{name} выбывает. Один кислородный паёк освободился. Прогресс.',
  'Прощай, {name}. Ты был... ну, ты был. Этого достаточно.',
  '{name} уходит. Демократия в действии — большинство всегда право. Почти.',
  'Бункер стал немного просторнее. Спасибо, {name}, за жертву. Добровольно-принудительную.',
  '{name} выбыл. Не грусти — снаружи хотя бы нет голосования.',
];

const ROUND_START_LINES = [
  'Раунд {round}. Кислорода всё меньше, нервов тоже. Продолжаем.',
  'Новый раунд. Для кого-то — последний. Но это потом.',
  'Раунд {round}. Те, кто остался — поздравляю. Пока что.',
  'Следующий раунд начинается. Помните, никто не в безопасности. Даже я.',
];

const FINALE_LINES = [
  'Выжили двое. Статистически это лучший результат для такой компании.',
  'Финал. Двое остались. Тринадцать начинали. Медицина потеряла многих.',
  'Два победителя в бункере, полном проигравших. Поэзия пандемии.',
  'Поздравляю выживших! Кислород — ваш. Вирус — тоже, но об этом потом.',
];

const TIE_LINES = [
  'Ничья! Даже в голосовании вы не можете определиться. Переголосование!',
  'Голоса разделились поровну. Как и мнения о вашей полезности.',
  'Ничья. Значит, будем голосовать снова. Демократия — штука упрямая.',
];

const PAUSE_TEXTS = [
  'Ведущий думает. Или паникует. Ждите.',
  'Пауза. Можете подышать. Пока есть чем.',
  'Техническая пауза. Кислород не расходуется... шучу.',
];

const SCORE_COMMENTS = {
  high: [
    '{name}, оценка {score}. Ты выжил и даже не опозорился. Редкость.',
    '{name} — {score} баллов. Бункер гордится тобой. Если бункер умеет гордиться.',
  ],
  mid: [
    '{name}, {score} баллов. Средненько, но для апокалипсиса сойдёт.',
    '{name} получает {score}. Не герой, не злодей. Просто... был.',
  ],
  low: [
    '{name}, {score} баллов. Ты ушёл рано, но зато первый подышал свежим воздухом.',
    '{name} — {score}. Короткое, но яркое пребывание в бункере.',
  ],
};

function pickRandom(arr) {
  return arr[Math.floor(Math.random() * arr.length)];
}

export class AIHost {
  constructor(apiKey = null) {
    this.apiKey = apiKey;
    this.useAPI = false;
  }

  async getWelcome(playerName) {
    if (this.useAPI) return this._callAPI(`Поприветствуй нового игрока ${playerName} с сарказмом`);
    return pickRandom(WELCOME_LINES).replace('{name}', playerName);
  }

  async getGameStart(playerNames) {
    if (this.useAPI) return this._callAPI(`Игра начинается. Игроки: ${playerNames.join(', ')}. Произнеси вступительный монолог.`);
    return pickRandom(GAME_START_LINES);
  }

  // Комментарий при вызове игрока
  async getPlayerCalled(playerName) {
    if (this.useAPI) return this._callAPI(`Ведущий вызывает игрока ${playerName}. Прокомментируй с сарказмом, 1-2 предложения.`);
    return pickRandom(PLAYER_CALLED_LINES).replace(/{name}/g, playerName);
  }

  // Реакция на открытую карту
  async getCardReaction(playerName, cardType, cardValue, profession) {
    if (this.useAPI) {
      return this._callAPI(
        `Игрок ${playerName} открыл карту "${cardType}": "${cardValue}". Его профессия: ${profession}. Прокомментируй с сарказмом, 1-2 предложения.`
      );
    }
    const typeKey = cardType === 'Профессия' ? 'profession' :
                    cardType === 'Биология' ? 'biology' :
                    cardType === 'Здоровье' ? 'health' :
                    cardType === 'Хобби' ? 'hobby' :
                    cardType === 'Факт' ? 'fact' :
                    cardType === 'Багаж' ? 'baggage' : 'profession';
    
    const reactions = CARD_TYPE_REACTIONS[typeKey] || CARD_TYPE_REACTIONS.profession;
    return pickRandom(reactions).replace(/{name}/g, playerName).replace(/{value}/g, cardValue);
  }

  // Когда игрок начинает оправдываться
  async getDefenseReaction(playerName) {
    if (this.useAPI) return this._callAPI(`${playerName} начинает оправдываться за свою карту. Прокомментируй ехидно, 1 предложение.`);
    return pickRandom(DEFENSE_REACTIONS).replace(/{name}/g, playerName);
  }

  // Когда ход завершён
  async getTurnEndReaction(playerName) {
    if (this.useAPI) return this._callAPI(`${playerName} закончил свой ход. Прокомментируй кратко и саркастично.`);
    return pickRandom(TURN_END_REACTIONS).replace(/{name}/g, playerName);
  }

  async getVoteStart(round) {
    if (this.useAPI) return this._callAPI(`Раунд ${round}. Начинается голосование. Подстрекай.`);
    return pickRandom(VOTE_START_LINES);
  }

  async getVoteProgress() {
    return pickRandom(VOTE_PROGRESS_LINES);
  }

  async getElimination(playerName, profession) {
    if (this.useAPI) return this._callAPI(`${playerName} (${profession}) вылетает. Попрощайся с сарказмом.`);
    return pickRandom(ELIMINATION_LINES).replace(/{name}/g, playerName);
  }

  async getRoundStart(round) {
    return pickRandom(ROUND_START_LINES).replace(/{round}/g, round);
  }

  async getTieReaction() {
    return pickRandom(TIE_LINES);
  }

  async getFinale(winner1, winner2) {
    if (this.useAPI) return this._callAPI(`Финал! Победители: ${winner1} и ${winner2}. Произнеси финальный монолог.`);
    return pickRandom(FINALE_LINES);
  }

  async getScoreComment(playerName, score) {
    const tier = score >= 93 ? 'high' : score >= 88 ? 'mid' : 'low';
    if (this.useAPI) return this._callAPI(`Выставь оценку ${playerName}: ${score} баллов. Прокомментируй.`);
    return pickRandom(SCORE_COMMENTS[tier]).replace(/{name}/g, playerName).replace(/{score}/g, score);
  }

  getPauseText() {
    return pickRandom(PAUSE_TEXTS);
  }

  async _callAPI(userMessage) {
    try {
      const Anthropic = (await import('@anthropic-ai/sdk')).default;
      const client = new Anthropic({ apiKey: this.apiKey });
      const response = await client.messages.create({
        model: 'claude-sonnet-4-20250514',
        max_tokens: 150,
        system: `Ты — ИИ-ведущий игры "Бункер 204-А". Ты хамоватый, саркастичный, с чёрным медицинским юмором. Обращаешься к игрокам по имени. Никогда не помогаешь — только издеваешься. Комментируешь абсурдность ситуации. Отвечай 1-3 предложениями. Только на русском.`,
        messages: [{ role: 'user', content: userMessage }],
      });
      return response.content[0].text;
    } catch (e) {
      console.error('AI API error:', e.message);
      return 'ИИ-ведущий молчит. Видимо, даже ему стало не по себе.';
    }
  }
}
